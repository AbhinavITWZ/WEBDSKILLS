//browser object model
//provided by the browser it provide the window object 
//its choice to write the window. or not but we can use that functions provided by the browsers 

// 1.location// we have also studied dialogue boxes
// location.reload();//infinite loop but helps reload [page]
// location.href="https://www.google.com"



// 2.history //another object in windows
// history.back();
// history.forward();
// history.go(takes parameter );//like history is of 3 thene -2 takes to first history page

//3.innerHeight and innerWidth
// 4.open("https://www.yt.com",name)//new tab//by default name empty string 
//5.close()//only closes the tab opened by open()


//open("link",name,"resizable");
//then only use resizeTo()

//resizeBy() resize corresponding size()


//moveTo()//move the popup to a particular heught and width
//moveBy() resize corresponding position()

//doucment is part of windows object(DOM)